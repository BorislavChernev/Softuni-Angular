import { ImageLinkDirective } from './image-link.directive';

describe('ImageLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLinkDirective();
    expect(directive).toBeTruthy();
  });
});
